{
  "name": "esbuild-ssr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && npm run generate-routes && ENV=production node build.mjs",
    "dev": "npm run clean && node build.mjs",
    "serve": "nodemon dist/entryServer.js --watch dist/entryServer.js",
    "generate-routes": "node create-routes.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mdx-js/esbuild": "^2.3.0",
    "compression": "^1.7.4",
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "glob": "^10.3.3",
    "react-router-dom": "^6.14.1"
  },
  "devDependencies": {
    "esbuild": "^0.18.9",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.1"
  }
}
